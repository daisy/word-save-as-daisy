<UserControl x:Class="Daisy.SaveAsDAISY.WPF.CustomControls.StringParameter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="300">
    <StackPanel>
        <Label Content="{Binding Path=ParameterName}"
               Margin="5,0,5,0"
               VerticalAlignment="Center" />
        <TextBox Text="{Binding Path=ParameterValue}"
                 Margin="5,0,5,0"
                 VerticalAlignment="Center"
                 AutomationProperties.Name="{Binding Path=ParameterName}"
                 AutomationProperties.HelpText="{Binding Path=ParameterDescription}" />
    </StackPanel>
</UserControl>
